# ─────────────────────────────────────────────
# Dependencias combinadas de techgear_api + techgear_web
# Render busca este archivo en la raiz del repositorio
# ─────────────────────────────────────────────

# --- API (FastAPI) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pymongo>=4.6
dnspython>=2.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.9

# --- Web (Django) ---
Django==4.2.30
gunicorn==22.0.0

# --- Compartidas ---
python-dotenv==1.0.1
requests==2.32.3
